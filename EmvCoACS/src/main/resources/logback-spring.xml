<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- ===================================================== -->
    <!-- Define common properties -->
    <!-- ===================================================== -->
    <property name="LOG_PATH" value="./logs" />
    <property name="APP_NAME" value="demo-app" />

    <!-- ===================================================== -->
    <!-- Console appender (optional - remove if you only want files) -->
    <!-- ===================================================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================================================== -->
    <!-- Rolling file appender for all logs -->
    <!-- ===================================================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Rotate daily -->
            <fileNamePattern>${LOG_PATH}/${APP_NAME}.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Keep logs for 14 days -->
            <maxHistory>14</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================================================== -->
    <!-- (Optional) Separate file appender for request/response logs -->
    <!-- ===================================================== -->
    <appender name="FILTER_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/filters.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/filters.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================================================== -->
    <!-- Log levels per package -->
    <!-- ===================================================== -->

    <!-- Application-wide default log level -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- Filter-specific log level (your filters) -->
    <logger name="com.example.demo.filter" level="DEBUG" additivity="false">
        <appender-ref ref="FILTER_FILE" />
    </logger>

</configuration>
